const serverBus = new Vue();

Vue.component("progress-bar", {
    });

const progressApp = new Vue({
    el: "#progressApp",
    data: {
    },
    computed: {
    },
    mounted: function() {
    },
    methods: {
    }
});